#include <iostream>
#include <string>
using namespace std;
 
int main() {
	string str;
	cin >> str;
	int k;
	cin >> k;
	for(int i = 0 ; i < str.length(); i++){
		if(str[i] == 'Z') str[i] = 'A';
		else str[i]+=k;
	}
	cout << str;
	return 0;
}
